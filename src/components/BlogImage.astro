---
export interface Props {
  src: ImageMetadata
  alt?: string
}

const { src, alt = "" } = Astro.props as Props
---

<img
  src={src.src}
  alt={alt}
  width={src.width}
  height={src.height}
  loading="lazy"
  decoding="async"
/>
